import L from 'leaflet';
import type { Map } from 'leaflet';
interface HeatPoint {
    lat: number;
    lng: number;
    intensity: number;
}
interface Props {
    leafletObject: Map;
    heatPoints: HeatPoint[];
    radius?: number;
}
export declare const useLHeat: (props: Props) => Promise<L.HeatLayer>;
export {};
